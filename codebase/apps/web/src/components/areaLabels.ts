import type { LessonArea } from '../content'

/** Rendered names for lesson areas (identifiers stay lowercase in code). */
export const AREA_LABELS: Record<LessonArea, string> = {
  scales: 'Scales',
  arpeggios: 'Arpeggios',
  chords: 'Chords',
  standards: 'Standards',
}
