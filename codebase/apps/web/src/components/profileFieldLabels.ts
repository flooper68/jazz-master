import type { PracticeArea } from '../storage'

export const AREA_LABELS: Record<PracticeArea, string> = {
  scales: 'Scales',
  arpeggios: 'Arpeggios',
  chords: 'Chords',
  standards: 'Standards',
  ears: 'Ear training',
}
