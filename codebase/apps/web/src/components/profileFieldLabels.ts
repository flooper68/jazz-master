import type { PracticeArea } from '../appData/profile'

export const AREA_LABELS: Record<PracticeArea, string> = {
  scales: 'Scales',
  arpeggios: 'Arpeggios',
  chords: 'Chords',
  standards: 'Standards',
  ears: 'Ear training',
}
