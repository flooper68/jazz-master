# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local secrets. `.env`/`.dev.vars` are also what `astro build` and the
# Cloudflare adapter read, which is why local values live in `.env.development`
# instead — see codebase/apps/web/README.md (TASK-086).
.env
.env.*
!.env*.example
.dev.vars
.dev.vars.*
node_modules
dist
dist-ssr
*.local

# Generated by astro dev/build and the Cloudflare adapter
.astro/
.wrangler/
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP browser artifacts
.playwright-mcp/

# Playwright e2e output (TASK-035)
test-results/
playwright-report/
